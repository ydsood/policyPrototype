import {renderComponent, expect} from './test_helper';
import ResultList from '../../src/components/result_list';

describe('Result List', () => {

    let component;

    beforeEach(()=>{
        component = renderComponent(ResultList);
    });
});